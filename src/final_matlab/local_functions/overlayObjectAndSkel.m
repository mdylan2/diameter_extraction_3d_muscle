function [outputArg1,outputArg2] = overlayObjectAndSkel(imLabel,imSkel,object_no)
%UNTITLED3 Summary of this function goes here
%   Detailed explanation goes here
% ro = regionprops3(imLabel,"Volume");
% volshow(sk)
% % Overlay volume viewer function
% merged = double(sk|bo); % overlay the skeleton and boundary
% label = merged; % make the label from the merged
% label(find(sk)) = 2; % make the locations at the skeleton 2
% volumeViewer(merged, label); % view the volume with labels
end

